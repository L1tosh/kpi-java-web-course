apiVersion: v1
kind: Service
metadata:
  name: ecommerce-app
  labels:
    app: ecommerce-app
spec:
  type: ClusterIP
  selector:
    app: ecommerce-app
  ports:
    - name: http
      port: 8080
      targetPort: http
